[project]
name = "workspace"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = ["marimo>=0.15.2"]

[tool.marimo.package_management]
manager = "uv"

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
